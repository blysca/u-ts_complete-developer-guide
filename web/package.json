{
  "scripts": {
    "start:parcel": "parcel index.html",
    "start:db": "json-server -w db.json",
    "start": "json-server -w db.json"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "axios": "^0.19.1"
  }
}
